import { useSSG } from 'nextra/ssg';
import screenshot from '../../../lib/screenshot';
import ScreenshotImage from '../../../components/ScreenshotImage';

export const getStaticProps = () => {
const url = 'https://moneysmart.gov.au/'

return screenshot(url).then((imageUrl) => ({
props: { ssg: {imageUrl} },
}))
};

export const Screenshot = () => {
const {imageUrl} = useSSG();

return <ScreenshotImage imageUrl={imageUrl}/>
}

# Money Smart

### About

Money Smart helps Australians take control of their money with free tools, tips and guidance.

### Operated by

Australian Securities and Investments Commission (ASIC)

### Link(s)

[https://moneysmart.gov.au/](https://moneysmart.gov.au/)

### Screenshot

<Screenshot />
