import { useSSG } from 'nextra/ssg';
import screenshot from '../../../lib/screenshot';

export const getStaticProps = () => {
const url = 'https://www.energymadeeasy.gov.au/'

return screenshot(url).then((imageUrl) => ({
props: { ssg: {imageUrl} },
}))
};

export const ScreenshotImage = () => {
const {imageUrl} = useSSG();

return <img src={imageUrl} alt="screenshot"/>
}

# Energy Made Easy

### About

Find the right energy plan for you. We will help you by comparing all the retailers

### Operated by

Australian Energy Regulator

### Link(s)

[https://www.energymadeeasy.gov.au/](https://www.energymadeeasy.gov.au/)

### Link(s)

<ScreenshotImage />
