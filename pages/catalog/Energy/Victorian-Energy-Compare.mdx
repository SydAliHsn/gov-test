import { useSSG } from 'nextra/ssg';
import screenshot from '../../../lib/screenshot'; import Image from 'next/image';

export const getStaticProps = () => {
const url = 'https://compare.energy.vic.gov.au/'

return screenshot(url).then((imageUrl) => ({
props: { ssg: {imageUrl} },
}))
};

export const ScreenshotImage = () => {
const {imageUrl} = useSSG();

return <div className="w-full aspect-video relative mt-8"><Image src={imageUrl} fill={true} alt="" /></div>
}

# Victorian Energy Compare

### About

An independent Victorian Government energy price comparison site.

### Operated by

Victoria State Government.

### Link(s)

[https://compare.energy.vic.gov.au/](https://compare.energy.vic.gov.au/)

### Screenshot

<ScreenshotImage />
