import { useSSG } from 'nextra/ssg';
import screenshot from '../../../lib/screenshot';
import ScreenshotImage from '../../../components/ScreenshotImage';

export const getStaticProps = () => {
const url = 'https://recovery.serviceconnect.gov.au/'

return screenshot(url).then((imageUrl) => ({
props: { ssg: {imageUrl} },
}))
};

export const Screenshot = () => {
const {imageUrl} = useSSG();

return <ScreenshotImage imageUrl={imageUrl}/>
}

{/* Actual Content of the page */}

# Recovery Connect

### About

Recovery Connect brings together services and supports from charities, organisations and government directly to you. This assistance helps people prepare for and recover from disasters and major natural events.

### Operated by

Australian Government.

### Link(s)

https://recovery.serviceconnect.gov.au/

### Screenshot

<Screenshot />
