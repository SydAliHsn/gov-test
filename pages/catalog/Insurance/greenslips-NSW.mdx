import { useSSG } from 'nextra/ssg';
import screenshot from '../../../lib/screenshot'; import Image from 'next/image';

export const getStaticProps = () => {
const url = 'https://www.greenslips.nsw.gov.au/'

return screenshot(url).then((imageUrl) => ({
props: { ssg: {imageUrl} },
}))
};

export const ScreenshotImage = () => {
const {imageUrl} = useSSG();

return <div className="w-full aspect-video relative mt-8"><Image src={imageUrl} fill={true} alt="" /></div>
}

# Greenslips NSW

### About

Green Slip Price Check is a quick and easy way to get a Green Slip quote comparison from all insurers. This tool is for NSW users.

### Operated by

State Insurance Regulatory Authority, NSW Government.

### Link(s)

[https://www.greenslips.nsw.gov.au/](https://www.greenslips.nsw.gov.au/)

### Screenshot

<ScreenshotImage />
