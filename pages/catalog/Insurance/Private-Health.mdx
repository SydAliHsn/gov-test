import { useSSG } from 'nextra/ssg';
import screenshot from '../../../lib/screenshot';
import ScreenshotImage from '../../../components/ScreenshotImage';

export const getStaticProps = () => {
const url = 'https://privatehealth.gov.au/'

return screenshot(url).then((imageUrl) => ({
props: { ssg: {imageUrl} },
}))
};

export const Screenshot = () => {
const {imageUrl} = useSSG();

return <ScreenshotImage imageUrl={imageUrl}/>
}

# Private Health

### About

Comprehensive, independent private health insurance information.

### Operated by

Commonwealth Ombudsman

### Link(s)

[https://www.privatehealth.gov.au/](https://www.privatehealth.gov.au/)

### Screenshot(s)

<Screenshot />
