import { useSSG } from 'nextra/ssg';
import screenshot from '../../../lib/screenshot';

export const getStaticProps = () => {
const url = 'https://trove.nla.gov.au/'

return screenshot(url).then((imageUrl) => ({
props: { ssg: {imageUrl} },
}))
};

export const ScreenshotImage = () => {
const {imageUrl} = useSSG();

return <img src={imageUrl} alt="screenshot"/>
}

# Trove

### About

Trove is an Australian online library database. Search Trove to explore amazing collections from Australian libraries, universities, museums, galleries and archives. It’s free and available online all day, every day.

### Operated by

National Library of Australia

### Link(s)

[https://trove.nla.gov.au/](https://trove.nla.gov.au/)

### Screenshot

<ScreenshotImage />
